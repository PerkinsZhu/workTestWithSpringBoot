server:
  port: 8088
  servlet:
    context-path:  /test
  tomcat:
    uri-encoding:  UTF-8

spring:
  application:
    name: srping boot
  datasource:
    url:  jdbc:mysql://127.0.0.1:3306/shiro?useUnicode=true&characterEncoding=UTF-8
    username:  root
    password:  root
    driver-class-name:  com.mysql.jdbc.Driver
    max-idle:  10
    max-wait:  10000
    min-idle:  5
    initial-size:  5

logging:
  config:
    classpath: pm-logback.xml
  path:  log